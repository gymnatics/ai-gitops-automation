# On the bastion, after pulling the changes, run:

chmod +x check-and-fix-cluster.sh
chmod +x check-cluster-status.sh

# Then you can run the script:
./check-and-fix-cluster.sh

# Or if you're not in the ai-gitops-automation directory:
cd ~/ai-gitops-automation
chmod +x *.sh
./check-and-fix-cluster.sh
